version: 0.2
phases:
  install:
     runtime-versions:
       python: 3.8       
build:
   commands:
      - aws cloudformation package --template-file samTemplate.yaml --s3-bucket test-lambdawidemo-artifacts --output-template-file ISamTemplate.yaml
artifacts:
  type: zip
  files:
    - samTemplate.yaml
    - ISamTemplate.yaml
